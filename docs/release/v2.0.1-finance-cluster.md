# PersianToolbox v2.0.1 - Finance Cluster Completion

Date: 2026-02-12
Target domain: `https://persiantoolbox.ir`

## Summary

This release finalizes PersianTools V2 finance scope (Phases 0-3) and keeps runtime local-first:

- Brand/canonical alignment to `persiantoolbox.ir`
- Local-first policy gate for external runtime restrictions
- Finance hub and SEO landings for loan/salary/interest
- Related-tools conversion layer and local saved calculations
- Authority pages (`/about`, `/how-it-works`) and trust blocks

## Runtime/Policy Guardrails

- Client-first, offline-friendly runtime
- No external scripts/APIs/CDN dependencies added
- Local-first scan enforced in CI (`pnpm gate:local-first`)
- New local build smoke gate (`pnpm smoke:local`)

## Verification Snapshot

- `pnpm run ci:quick` passed
- `pnpm run build` passed
- `pnpm run smoke:local` passed

## Release Notes

- Currency display for finance tools remains TOMAN in UI.
- Finance internal-linking matrix is in place via hub and related-tools blocks.
- Structured data coverage remains enabled through existing SEO schema utilities.

## Rollback

- Use standard rollback procedure documented in `ops/deploy/rollback.sh`.
- Keep previous stable tag (`v2.0.0`) as fallback baseline.
